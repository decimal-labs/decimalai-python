"""Integrations package — framework adapters for trace capture.

The canonical import path for the LangChain adapter is now
``decimalai.langchain``. This package ships the helper modules those
adapters build on: ``huggingface``, ``otel``, ``langchain_introspect``,
``openai_agents_introspect`` and ``_lc_compat``.
"""
